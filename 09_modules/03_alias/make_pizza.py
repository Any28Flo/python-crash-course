"""
Sometimes the name of the function is to large o x reason.
For this circuntances we could import the functions with
aliases.
An alias is a nickname for that function
"""
from pizza import make_pizza as mp

mp('Small', 'cheese', 'peperoni', 'mushrooms')
